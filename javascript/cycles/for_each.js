const elements = ["button", "input", "radio", "checkbox"];

elements.forEach((element) => {
  // * Sipkova funkce
  console.log("Prvek: " + element);
}); // * forEach metoda vola funkci pro kazdy prvek pole. Pouziva se pro seznamy hodnot.
